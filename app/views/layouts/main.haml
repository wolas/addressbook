!!!
%html
  %head
    %title
      AddressBook
      - if logged_in?
        \-
        = current_user.name

    = stylesheet_link_tag 'jquery', 'main'
    = javascript_include_tag :defaults

  %body
    - if logged_in?
      #menu
        - if admin?
          = link_to_unless_current 'Users', users_url
          |
          = link_to_unless_current 'Companies', companies_url
          |
          = link_to_unless_current 'Roles', roles_path
          |
          = link_to_unless_current current_user.company.name, current_user.company

        - else
          = link_to_unless_current current_user.company.name, current_user.company

      #user_badge.ui-widget-content.widget
        .two_col
          = link_to image_tag(gravatar_url(current_user.email)), current_user
        .two_col
          = link_to 'Logout', logout_url
          |
          = link_to 'My Account', account_url

    - else
      #user_badge.ui-widget-content.widget
        = link_to_unless_current 'Register', new_user_path
        |
        = link_to_unless_current 'Login', new_user_session_path
    .clear

    .flash
      - if notice = flash[:notice]
        .ui-state-highlight.widget
          %p
            %span{:class => "ui-icon ui-icon-info", :style => "float: left; margin-right: 0.3em;"}
            = notice

      - if error = flash[:error]
        .ui-state-error.widget
          %p
            %span{:class => "ui-icon ui-icon-alert", :style => "float: left; margin-right: 0.3em;"}
            = error

    #content= yield
